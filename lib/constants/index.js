const LOG_TYPE_SUCCESS = 'success'
const LOG_TYPE_ERROR = 'error'
const LOG_TYPE_WARNING = 'warning'
const LOG_TYPE_INFO = 'info'

const CMD_RUN_BACKEND = 'npm run backend:start'
const CMD_OPEN_INTERFACE = 'npm run interface:start'

module.exports = {
  LOG_TYPE_SUCCESS,
  LOG_TYPE_ERROR,
  LOG_TYPE_WARNING,
  LOG_TYPE_INFO,
  CMD_RUN_BACKEND,
  CMD_OPEN_INTERFACE
}
